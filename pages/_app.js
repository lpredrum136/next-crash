// import '../styles/globals.css'
import 'bootstrap/dist/css/bootstrap.min.css' // use bootswatch,
// download bootstrap.min.css and overwrite to change this bootstrap.min.css to get new theme
// must restart server

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
